#include <stdio.h>
#include <stdlib.h>
#include "ALU.h"
unsigned float ADD(unsigned long int R1, unsigned long int R2)
{
    return (R1+R2);
}
unsigned float SUM(unsigned long int R1, unsigned long int R2);
{
    return(R1-R2);
}
unsigned float BIC(unsigned long int R1, unsigned long int R2);
{
    return(R1&R2);
}
